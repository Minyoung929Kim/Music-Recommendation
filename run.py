from app.main import app

if __name__ == "__main__":
    app.run()

#just import the app, which is the actual flask app that we created that has all the information, then we fun the app, websiet is fontend, and what we are working on now is the backend
#write the part when we need prediction